# GitHub setup

```bash
git init
git add .
git commit -m "Initial Aurevane luxury storefront"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/aurevane-luxury-store.git
git push -u origin main
```

Do NOT commit `.env`.
